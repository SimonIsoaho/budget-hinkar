# Period, Transfer & Activity Implementation Plan

**Goal:** Home period summary (25–24), transfer between buckets, latest-5 activity feed.

Done in one pass: migration `transfer_id`, period helpers, household queries, TransferModal, Home UI.
